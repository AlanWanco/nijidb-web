[project]
name = "nijidb-web"
version = "0.1.0"
description = "Local Nijigasaki music database and update notifier"
requires-python = ">=3.11"
dependencies = [
  "boto3>=1.35,<2",
  "beautifulsoup4>=4.12,<5",
  "fastapi>=0.115,<1",
  "httpx>=0.27,<1",
  "uvicorn[standard]>=0.30,<1",
]

[tool.uv]
package = false
